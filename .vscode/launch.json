{
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "node",
            "preLaunchTask": "build"
        }
    ]
}